/*  const authCheck = useSelector(
    (state: AppState) => state.user.userName
  ); */

/* const getProducts = async () => {
    await axios.get(`http://localhost:9000/api/v1/products`, {
      headers: {
        authorization: `Bearer ${authCheck}`,
      },
    });
  }; */
